import { handleWebRequest } from './webServer';
import { handleHttpMockRequest } from './apiServer';
import { handleSystemRequest } from './systemServer';

export {
  handleWebRequest,
  handleHttpMockRequest,
  handleSystemRequest,
};